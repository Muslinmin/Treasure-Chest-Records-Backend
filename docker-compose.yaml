version: '3.8'

services:
  api:
    build: .
    container_name: fastapi-backend
    restart: unless-stopped
    env_file:
      - ./data/.env
    volumes:
      - ./data:/app/data
    ports:
      - "127.0.0.1:8000:8000"